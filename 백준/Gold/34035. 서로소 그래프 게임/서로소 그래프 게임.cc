#include <bits/stdc++.h>
#define all(v) v.begin(), v.end()

using namespace std;
using ll = long long;

ll n, a, b, c, d, f, k, p, e, q, m;

void solve() {
    cin >> n;
    if(n == 2) cout << "kang\n";
    else cout << "chan\n";
}

int main() {
    ios::sync_with_stdio(false); cin.tie(0);
    ll tc = 1;
    cin >> tc;
    while (tc--) {
        solve();
    }
    return 0;
}